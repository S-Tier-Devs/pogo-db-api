# Changelog

All user-visible changes, newest first. A release PR bumps the version and adds its
section here in the same commit. "User-visible" includes the shape of the JSON served
from `public/api/` — there is no URL versioning, so field renames and removals are
breaking changes.

<!-- Started at the ai-standards retrofit; entries before 1.0.0 were not tracked
here. See git history for anything earlier. -->

## [1.0.0] — unreleased
- Existing baseline at the time the changelog was added.
